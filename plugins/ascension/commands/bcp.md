---
description: Set up or operate a Brand Context Protocol (BCP) — capture brand truth, produce on-brand output, score it
argument-hint: [scaffold | <deliverable or brand task>]
---
Load and follow the `bcp` skill to bring a Brand Context Protocol into the work.

Request: $ARGUMENTS

Find or scaffold the brand repo (github.com/mtthsnc/bcp), then route to BCP's own skills —
`brand-truth` to capture/extend the source, `landing-page` (or similar) to produce a traced
deliverable, and `brand-check` to score it before shipping. Don't invent brand facts here; the BCP
repo is the source of truth.
